# Frontend Boundary

The final React UI is outside Phase 2 business scope.
This directory reserves the decoupled frontend boundary defined in Phase 1.1.

Target frontend stack:
- React
- TypeScript
- Tailwind CSS

The frontend communicates with Laravel only through the versioned REST API.
